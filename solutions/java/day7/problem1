class Solution {
    public int countDigits(int num) {
        int cnt = 0;
        int k = num;
        while(num != 0){
            int temp = num % 10;
            if(k % temp==0) cnt++;
            num /= 10;
        }

        return cnt;
    }
}