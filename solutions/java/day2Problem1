class Solution {
    public boolean isPalindrome(int x) {
        int k = x;
        long res = 0;
        while(x!=0){
            res += x%10;
            x /= 10;
            res *= 10;
        }

        res /= 10;
        return k>=0 && res == k;
    }
}