class Solution {
    //if we write for each n in a piece of paper, we notice a pattern which shows that to make an every array equal
    //we need to look at the previous one plus current n/2, that is why I used recursion in this case.
    public int minOperations(int n) {
        if(n == 1)return 0;
        return n/2 + minOperations(n-1);
    }
}